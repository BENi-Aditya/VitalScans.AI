Flask==2.3.2              # For creating and running the web application
Pillow==10.0.0            # For image processing (PIL library)
python-dotenv==1.0.0      # For loading environment variables from a .env file
inference-sdk==0.1.0      # Replace with the exact version of the Inference SDK you are using
datetime                  # Included in the Python standard library, no need to install
chatbot                   # Ensure the chatbot package is available in your environment or custom implementation
